{
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.9.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.53.0",
    "jest": "^29.7.0",
    "prettier": "^3.1.0",
    "ts-jest": "^29.1.1",
    "tsc-alias": "^1.8.8",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "rm -rf dist && tsc && tsc-alias",
    "watch": "tsc && (concurrently \"tsc --watch\" \"tsc-alias --watch\")",
    "test": "jest"
  },
  "type": "module",
  "dependencies": {
    "fp-ts": "^2.16.1"
  }
}
